# OS / editor
.DS_Store
.idea/
*.swp

# Claude Code local config (may contain machine-specific settings)
.claude/
.agents/
skills-lock.json

# Node / tooling
node_modules/
*.log

# Local env / secrets — never commit
.dev.vars
.env
.env.*
